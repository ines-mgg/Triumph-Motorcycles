{
  "name": "triumph_motorcycles",
  "private": true,
  "version": "0.0.0",
  "type": "commonjs",
  "config": {
    "commitizen": {
      "path": "git-cz"
    }
  },
  "lint-staged": {
    "*.{js,jsx,ts,tsx, json}": [
      "eslint --no-warn-ignored --max-warnings 0 .",
      "npm run prettier"
    ],
    "*.css": "stylelint"
  },
  "workspaces": [
    "src/*"
  ],
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "clean": "rm -rf node_modules",
    "cm": "git cz",
    "cm:retry": "git cz --retry",
    "cy:open": "npx cypress open",
    "lint:css": "stylelint \"src/**/*.css\"",
    "lint:js": "eslint . --ext .jsx,.js,.tsx,.ts,.json",
    "lint:types": "tsc --noemit -p tsconfig.json --module esnext",
    "lint": "npm run lint:css && npm run lint:js",
    "reinstall": "npm run clean && npm install",
    "rebuild": "npm run clean && npm install && npm run build",
    "preview": "vite preview",
    "prepare": "husky",
    "precommit": "npx lint-staged",
    "prepush": "npm run lint:types && npm run test:unit",
    "prettier": "npx prettier . --write --ignore-unknown",
    "postmerge": "npm install",
    "postrewrite": "npm install",
    "test:unit": "npm run lint:types && npm run test",
    "test": "vitest"
  },
  "dependencies": {
    "commitizen": "^4.3.1",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@eslint/js": "^9.11.1",
    "@types/react": "^18.3.10",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.2",
    "cypress": "^13.15.0",
    "eslint": "^9.11.1",
    "eslint-config-prettier": "^9.1.0",
    "eslint-plugin-react-hooks": "^5.1.0-rc.0",
    "eslint-plugin-react-refresh": "^0.4.12",
    "git-cz": "^4.9.0",
    "globals": "^15.9.0",
    "husky": "^9.1.6",
    "lint-staged": "^15.2.10",
    "stylelint": "^16.9.0",
    "stylelint-config-recess-order": "^5.1.1",
    "stylelint-config-standard": "^36.0.1",
    "stylelint-no-unsupported-browser-features": "^8.0.1",
    "typescript": "^5.6.2",
    "typescript-eslint": "^8.7.0",
    "vite": "^5.4.8",
    "vitest": "^2.1.1"
  }
}
